<Page
    x:Class="NutzMich.Shared.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich.Shared.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="White" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="email" Header="E-mail" Grid.Row="0" Text="{Binding Email, Mode=TwoWay}"/>
        <PasswordBox x:Name="password" Header="Passwort" Grid.Row="1" Password="{Binding Password, Mode=TwoWay}"/>
        <Button Content="Login" Click="Login" Grid.Row="2"/>
    </Grid>
</Page>

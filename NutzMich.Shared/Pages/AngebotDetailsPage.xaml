<Page
    x:Class="NutzMich.Shared.Pages.AngebotDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich.Shared.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" x:Name="BackButton" Click="Back_Click" Style="{StaticResource NavigationBackButtonNormalStyle}"/>

        <Pivot Grid.Row="1" Title="{Binding Angebot.Ueberschrift, Mode=OneWay}">
            <PivotItem Header="Details">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <FlipView Grid.Row="0" ItemsSource="{Binding Fotos, Mode=OneWay}" MaxWidth="500" MaxHeight="250">
                        <FlipView.ItemTemplate>
                            <DataTemplate>
                                <Image Source="{Binding}" MaxHeight="500" MaxWidth="250" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </FlipView.ItemTemplate>
                    </FlipView>
                    <TextBox Grid.Row="1" Text="{Binding Angebot.Beschreibung, Mode=OneWay}" Header="Beschreibung" IsReadOnly="True" AcceptsReturn="True"/>
                    <TextBox Grid.Row="2" Text="{Binding Angebot.Zustand, Mode=OneWay}" Header="Zustand" IsReadOnly="True" AcceptsReturn="True"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="Anfrage">
                <Button Content="Anfrage stellen" Click="AnfrageStellen"/>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

<Page
    x:Class="NutzMich.Shared.Pages.AngebotDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich.Shared.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" x:Name="BackButton" Click="Back_Click" Style="{StaticResource NavigationBackButtonNormalStyle}"/>

        <Pivot Grid.Row="1" Title="{Binding Angebot.Ueberschrift, Mode=OneWay}">
            <PivotItem Header="Details">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Angebot.Beschreibung, Mode=OneWay}" />
                    <FlipView Grid.Row="1" ItemsSource="{Binding Fotos, Mode=OneWay}" MaxWidth="500" MaxHeight="500">
                        <FlipView.ItemTemplate>
                            <DataTemplate>
                                <Image Source="{Binding}" MaxHeight="500" MaxWidth="500"/>
                            </DataTemplate>
                        </FlipView.ItemTemplate>
                    </FlipView>
                </Grid>
            </PivotItem>
            <PivotItem Header="Anfrage">
                <TextBlock Text="Anfragen..."/>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

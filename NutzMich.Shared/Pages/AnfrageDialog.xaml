<ContentDialog
    x:Class="NutzMich.Shared.Pages.AnfrageDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich.Shared.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Anfrage stellen"
    PrimaryButtonText="Anfrage stellen"
    SecondaryButtonText="Abbrechen"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0">
            <DatePicker Date="{Binding Anfrage.LeiheVon, Mode=TwoWay}" Header="Leihen von"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1">
            <DatePicker Date="{Binding Anfrage.LeiheBis, Mode=TwoWay}" Header="Leihen bis"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="2">
            <DatePicker Date="{Binding Anfrage.AbholungsZeitpunkt, Mode=TwoWay}" Header="Abholung"/>
            <TimePicker Time="{Binding Anfrage.AbholungsZeitpunkt, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</ContentDialog>

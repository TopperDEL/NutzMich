<Page
    x:Class="NutzMich.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:NutzMich.Shared.ViewModels"
    mc:Ignorable="d">

    <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Title="NutzMich">
        <PivotItem Header="Alle Angebote">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ProgressRing Grid.Row="0" Visibility="{x:Bind _angeboteVM.Loading, Mode=OneWay, Converter={StaticResource TrueIsVisibleConverter}}" IsActive="True"/>
                <ListView Grid.Row="1" ItemsSource="{x:Bind _angeboteVM.AlleAngebote, Mode=OneWay}" IsItemClickEnabled="True" ItemClick="AngebotAnzeigen"
                          Visibility="{x:Bind _angeboteVM.Loading, Mode=OneWay, Converter={StaticResource TrueIsCollapsedConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:AngebotViewModel">
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <Image Source="{x:Bind Thumbnail}" Width="100" Margin="10,0,10,0"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{x:Bind Path=Angebot.Ueberschrift}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Path=Angebot.Beschreibung}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </PivotItem>
        <PivotItem Header="Meine Angebote">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Click="NeuesAngebotAnlegen">
                    <Button.Content>
                        <TextBlock Text="+" FontSize="60"/>
                    </Button.Content>
                </Button>
                <ListView Grid.Row="1" ItemsSource="{x:Bind _angeboteVM.MeineAngebote, Mode=OneWay}"
                          Visibility="{x:Bind _angeboteVM.Loading, Mode=OneWay, Converter={StaticResource TrueIsCollapsedConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:AngebotViewModel">
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <Image Source="{x:Bind Thumbnail}" Width="100" Margin="10,0,10,0"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{x:Bind Path=Angebot.Ueberschrift}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Path=Angebot.Beschreibung}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ProgressRing Grid.Row="1" Visibility="{x:Bind _angeboteVM.Loading, Mode=OneWay, Converter={StaticResource TrueIsVisibleConverter}}" IsActive="True"/>
            </Grid>
        </PivotItem>
    </Pivot>
</Page>

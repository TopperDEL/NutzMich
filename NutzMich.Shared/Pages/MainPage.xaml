<Page
    x:Class="NutzMich.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:NutzMich.Shared.ViewModels"
    xmlns:xamarin="http://uno.ui/xamarin"
    xmlns:toolkit="using:Uno.UI.Toolkit"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0" Background="RoyalBlue" Foreground="White">
            <CommandBar.Content>
                <TextBlock Text="NutzMich" Margin="12"/>
            </CommandBar.Content>
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Refresh" Click="Refresh" Foreground="White"/>
                <AppBarButton Icon="Message" Click="OpenChats" Foreground="White"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
        <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Grid.Row="1">
            <PivotItem Header="Übersicht">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ProgressRing Grid.Row="0" Visibility="{Binding Loading, Mode=OneWay, Converter={StaticResource TrueIsVisibleConverter}}" IsActive="True" Foreground="RoyalBlue"/>
                    <ListView Grid.Row="1" ItemsSource="{x:Bind _angeboteVM.AlleAngebote, Mode=OneWay}" IsItemClickEnabled="True" ItemClick="AngebotAnzeigen">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:AngebotViewModel">
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <Image Source="{Binding Thumbnail, Mode=OneWay}" Width="100" Margin="0,0,10,0"/>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{x:Bind Path=Angebot.Ueberschrift}" FontWeight="Bold" Foreground="{x:Bind Path=VerfuegbarkeitsAmpel, Mode=OneWay}"/>
                                        <TextBlock Text="{x:Bind Path=BeschreibungShort}" TextWrapping="Wrap" MaxWidth="190"/>
                                        <TextBlock Text="{x:Bind Path=VerfuegbarkeitsDetails, Mode=OneWay}" TextWrapping="Wrap" MaxWidth="190" Foreground="{x:Bind Path=VerfuegbarkeitsAmpel, Mode=OneWay}"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
            <PivotItem Header="Meine Angebote">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Click="NeuesAngebotAnlegen" HorizontalAlignment="Center" Background="Transparent">
                        <Button.Content>
                            <Border CornerRadius="10" BorderThickness="2" Padding="10" BorderBrush="RoyalBlue">
                                <SymbolIcon Symbol="Add"/>
                            </Border>
                        </Button.Content>
                    </Button>
                    <ProgressRing Grid.Row="1" Visibility="{Binding Loading, Mode=OneWay, Converter={StaticResource TrueIsVisibleConverter}}" IsActive="True" Foreground="RoyalBlue"/>
                    <ListView Grid.Row="2" ItemsSource="{x:Bind _angeboteVM.MeineAngebote, Mode=OneWay}" IsItemClickEnabled="True" ItemClick="AngebotBearbeiten">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:AngebotViewModel">
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <Image Source="{x:Bind Thumbnail, Mode=OneWay}" Width="100" Margin="0,0,10,0"/>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{x:Bind Path=Angebot.Ueberschrift}" FontWeight="Bold"/>
                                        <TextBlock Text="{x:Bind Path=BeschreibungShort}" TextWrapping="Wrap" MaxWidth="190"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

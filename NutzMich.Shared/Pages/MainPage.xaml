<Page
    x:Class="NutzMich.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NutzMich"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:NutzMich.Shared.ViewModels"
    mc:Ignorable="d">

    <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Title="NutzMich">
        <PivotItem Header="Alle Angebote">
            <Grid>
                <ProgressRing Visibility="{x:Bind _angeboteVM.Loading, Mode=TwoWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <ListView ItemsSource="{x:Bind _angeboteVM.AlleAngebote}" IsItemClickEnabled="True" ItemClick="AngebotAnzeigen"
                      Visibility="{x:Bind _angeboteVM.Loading, Mode=TwoWay, Converter={StaticResource BooleanToInvertedVisibilityConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:AngebotViewModel">
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <Image Source="{x:Bind Thumbnail}" Width="100" Margin="10,0,10,0"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{x:Bind Path=Angebot.Ueberschrift}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Path=Angebot.Beschreibung}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </PivotItem>
        <PivotItem Header="Meine Angebote">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                <Button Click="NeuesAngebotAnlegen">
                    <Button.Content>
                        <TextBlock Text="+" FontSize="60"/>
                    </Button.Content>
                </Button>
                <ProgressRing Visibility="{x:Bind _angeboteVM.Loading, Mode=TwoWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <ListView ItemsSource="{x:Bind _angeboteVM.MeineAngebote}"
                          Visibility="{x:Bind _angeboteVM.Loading, Mode=TwoWay, Converter={StaticResource BooleanToInvertedVisibilityConverter}}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:AngebotViewModel">
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <Image Source="{x:Bind Thumbnail}" Width="100" Margin="10,0,10,0"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{x:Bind Path=Angebot.Ueberschrift}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Path=Angebot.Beschreibung}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </PivotItem>
    </Pivot>
</Page>
